import 'package:flutter/material.dart';

class AddAnalysis extends StatelessWidget {
  const AddAnalysis({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('dkfj'),
    );
  }
}